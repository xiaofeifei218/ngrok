# ngrokd 服务器配置示例
# 复制此文件为 .env 并修改配置

# ============================================
# 必填配置
# ============================================

# 服务器域名（必须配置）
DOMAIN=ngrok.example.com

# ============================================
# TLS 证书配置（生产环境必需）
# ============================================

# TLS 私钥路径（容器内路径）
TLS_KEY=/app/certs/server.key

# TLS 证书路径（容器内路径）
TLS_CRT=/app/certs/server.crt

# ============================================
# 端口配置（可选）
# ============================================

# 以下是容器内部 ngrokd 程序监听的端口
# 通常不需要修改这些值！

# HTTP 隧道监听地址（容器内部）
HTTP_ADDR=:80

# HTTPS 隧道监听地址（容器内部）
HTTPS_ADDR=:443

# 客户端连接端口（容器内部）
TUNNEL_ADDR=:4443

# ============================================
# 主机端口映射（docker-compose 使用）
# ============================================

# 注意：当前 docker-compose.yml 中端口映射是硬编码的
# 这些变量在示例中作为参考，需要手动修改 docker-compose.yml 使用

# 如果主机端口被占用，请直接修改 docker-compose.yml 的 ports 部分
# 例如: 将 "80:80" 改为 "8080:80"

# 主机端口映射（参考值）
HTTP_PORT=80          # 主机端口 → 容器 80 端口
HTTPS_PORT=443        # 主机端口 → 容器 443 端口
TUNNEL_PORT=4443      # 主机端口 → 容器 4443 端口

# 端口映射格式: "主机端口:容器端口"
# 例如: "8080:80" 表示主机 8080 端口映射到容器 80 端口
#
# 详细说明请查看: docs/PORT_CONFIGURATION.md

# ============================================
# 开发环境配置
# ============================================

# 开发环境建议配置:
# DOMAIN=ngrok.me
# HTTP_PORT=8080
# HTTPS_PORT=8443
# TUNNEL_PORT=4443
# TLS_KEY=
# TLS_CRT=

# 记得在 /etc/hosts 添加:
# 127.0.0.1 ngrok.me
# 127.0.0.1 test.ngrok.me
